# The wrapper scripts are executed by /bin/sh on Linux CI: CRLF here makes them
# fail to start. Pin them regardless of the contributor's core.autocrlf setting.
mvnw        text eol=lf
gradlew     text eol=lf
mvnw.cmd    text eol=crlf
gradlew.bat text eol=crlf
*.jar       binary
